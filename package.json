{
  "name": "@fuzzy-street/errors",
  "version": "0.1.0",
  "description": "Type-safe custom error classes with full context support",
  "main": "dist/main.js",
  "module": "dist/main.mjs",
  "types": "dist/main.d.ts",
  "type": "module",
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ],
  "keywords": [
    "error",
    "typescript",
    "context"
  ],
  "author": "aFuzzyBear",
  "license": "MIT",
  "packageManager": "pnpm@10.7.1",
  "scripts": {
    "dev": "tsx --watch --no-cache src/main.ts",
    "build": "tsup",
    "examples": "tsx --no-cache src/examples.ts",
    "test": "node --import tsx --experimental-test-coverage --test-reporter=spec  --test \"src/**/*.test.ts\"",
    "test:watch": "node --import tsx --test-reporter=spec  --test --watch \"src/**/*.test.ts\" \"tests/**/*.test.ts\"",
    "check": "tsc --noEmit",
    "lint": "biome lint --fix",
    "format": "biome format --fix",
    "setup-hooks": "node setup-git-hooks.js",
    "postinstall": "node -e \"try { require('./setup-git-hooks.js') } catch(e) { console.log('Git hooks setup skipped.') }\"",
    "release": "standard-version",
    "release:minor": "standard-version --release-as minor",
    "release:patch": "standard-version --release-as patch",
    "release:major": "standard-version --release-as major"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "@commitlint/cli": "^19.8.0",
    "@commitlint/config-conventional": "^19.8.0",
    "@types/node": "^22.14.0",
    "lint-staged": "^15.5.0",
    "standard-version": "^9.5.0",
    "tsup": "^8.4.0",
    "tsx": "^4.19.3",
    "typescript": "^5.8.2"
  },
  "directories": {
    "doc": "docs"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/fuzzy-st/errors.git"
  },
  "bugs": {
    "url": "https://github.com/fuzzy-st/errors/issues"
  },
  "homepage": "https://github.com/fuzzy-st/errors#readme"
}